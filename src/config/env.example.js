/**
 * 运行时环境变量模板
 *
 * 使用方法：
 * 1. 复制为同目录下的 env.js
 * 2. 按你的本地环境填写地址和密钥
 * 3. 不要把真实密钥提交到仓库
 */
window.__PAIR_ENV__ = {
  COMFY_HOST: "http://127.0.0.1:8188/",
  COMFY_API_KEY: "",
  CMS_UPLOAD_ENDPOINT: "",
  CMS_API_TOKEN: "",
  RACK_PROXY_BASE_URL: "http://127.0.0.1:8787"
};
